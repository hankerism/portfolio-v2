/* PostCSS — Tailwind CSS v4 is a single PostCSS plugin (no tailwind.config.js).
 * All design-system configuration lives in src/app/globals.css via @theme. */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
